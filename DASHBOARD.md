# ローカルWebダッシュボード

`news_log.xlsx` を読み込んで、通知済みニュースをブラウザで確認するための軽量ダッシュボードです。

## 起動方法

```bat
run_dashboard.bat
```

または直接実行します。

```bash
python dashboard.py
```

起動すると通常は `http://127.0.0.1:8501` がブラウザで開きます。8501番が使用中の場合は、8502以降の空きポートを自動で使います。

## できること

- 総件数、表示件数、平均重要度、最新通知日時の確認
- タイトル、要約、カテゴリの横断検索
- カテゴリ別フィルタ
- 最低重要度スコアでの絞り込み
- 通知日の範囲指定
- 新しい順、重要度順の並び替え
- カテゴリ別件数グラフ
- 重要度分布グラフ
- 記事カードから元記事を開く
- 60秒ごとの自動再読み込み

## オプション

別のExcelファイルを読みたい場合:

```bash
python dashboard.py --file path\to\news_log.xlsx
```

ブラウザを自動で開かない場合:

```bash
python dashboard.py --no-browser
```
